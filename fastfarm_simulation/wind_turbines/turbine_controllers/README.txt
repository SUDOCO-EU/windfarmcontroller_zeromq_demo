The libdiscon.T1.so and libdiscon.T2.so libraries are the compiled ROSCO turbine controller libraries. The file are usually identical copies of the same file, but require to be separate files because they need to be called independently from one another for the FAST.Farm simulation. Follow
the instructions here to install and compile ROSCO: https://github.com/nrel/ROSCO.
